// src/theme.ts
/**
 * @deprecated Không sử dụng file này nữa. Thay vào đó, hãy sử dụng src/styles/theme.ts
 */
import { createAntdTheme } from './styles/theme';

const theme = createAntdTheme(false);

export default theme;

